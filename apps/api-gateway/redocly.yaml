extends:
  - recommended

rules:
  # Auth is enforced globally by Clerk middleware — not per-operation security schemes.
  security-defined: off
  # operationId and 4xx responses are useful but not blocking for now.
  operation-operationId: warn
  operation-4xx-response: warn
