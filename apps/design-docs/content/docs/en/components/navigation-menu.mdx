---
title: "Navigation Menu"
description: "Horizontal navigation with flyout content panels, built with native elements and Base UI."
---

## Demo

<ComponentPreview name="navigation-menu-demo" className="min-h-[300px] flex justify-center items-start" />


## Usage

```tsx
import {
  NavigationMenu,
  NavigationMenuList,
  NavigationMenuItem,
  NavigationMenuTrigger,
  NavigationMenuContent,
  NavigationMenuLink,
  NavigationMenuIndicator,
  NavigationMenuViewport,
  navigationMenuTriggerStyle,
} from "@nebutra/ui/primitives"
```

## Props

### NavigationMenu (Root)

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `orientation` | `"horizontal" \| "vertical"` | `"horizontal"` | Menu orientation |
| `className` | `string` | — | Additional CSS classes |
| `children` | `ReactNode` | — | Menu content |

### NavigationMenuTrigger

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `className` | `string` | — | Additional CSS classes |
| `children` | `ReactNode` | — | Trigger label |

### NavigationMenuContent

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `className` | `string` | — | Additional CSS classes |
| `children` | `ReactNode` | — | Flyout panel content |

### NavigationMenuLink

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `active` | `boolean` | `false` | Marks as current page |
| `asChild` | `boolean` | `false` | Merge props onto child element |
| `className` | `string` | — | Additional CSS classes |
| `children` | `ReactNode` | — | Link content |

## Accessibility

- Built with native elements and Base UI — follows the WAI-ARIA [Disclosure Navigation pattern](https://www.w3.org/WAI/ARIA/apg/patterns/disclosure/examples/disclosure-navigation/).
- `NavigationMenuList` has `role="menubar"`.
- Triggers with flyout content have `aria-expanded` and `aria-haspopup="true"`.
- Active links have `aria-current="page"`.
- Keyboard: `Tab` to navigate items, `Enter`/`Space` to open flyout, `Escape` to close.
