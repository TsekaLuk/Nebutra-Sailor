---
title: "Radio Group"
description: "Single-choice selection control with circular radio button indicators."
---

## Demo

<ComponentPreview name="radio-group-demo" className="max-w-[400px]" />


## Usage

```tsx
import { RadioGroup, RadioGroupItem } from "@nebutra/ui/primitives"
```

## Props

### RadioGroup (Root)

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `string` | — | Controlled selected value |
| `defaultValue` | `string` | — | Uncontrolled default selected value |
| `onValueChange` | `(value: string) => void` | — | Callback when selection changes |
| `disabled` | `boolean` | `false` | Disables all radio items |
| `orientation` | `"horizontal" \| "vertical"` | `"vertical"` | Layout direction |
| `className` | `string` | — | Additional CSS classes |

### RadioGroupItem

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `string` | — | **Required.** Value for this option |
| `disabled` | `boolean` | `false` | Disables this specific item |
| `id` | `string` | — | For linking with a `<Label>` |
| `className` | `string` | — | Additional CSS classes |

## Examples

### Horizontal Layout

<ComponentPreview name="radio-group-2-demo" className="max-w-[400px]" />


### Controlled

<ComponentPreview name="radio-group-3-demo" className="max-w-[400px]" />


## Accessibility

- Built on Radix UI RadioGroup — has `role="radiogroup"`.
- Each item has `role="radio"` with `aria-checked`.
- Always pair items with `<Label>` via matching `id`/`htmlFor`.
- Keyboard: `ArrowUp`/`ArrowDown` (or `ArrowLeft`/`ArrowRight`) to navigate, `Space` to select.
