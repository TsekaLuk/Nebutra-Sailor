---
title: "Switch"
description: "Toggle on/off control for binary settings, built on Radix UI."
---


<ComponentPreview>
  <div className="flex items-center space-x-2">
    <Switch id="airplane-mode" />
    <Label htmlFor="airplane-mode">Airplane Mode</Label>
  </div>

  ```tsx
<div className="flex items-center space-x-2">
    <Switch id="airplane-mode" />
    <Label htmlFor="airplane-mode">Airplane Mode</Label>
  </div>
  ```
</ComponentPreview>

## Usage

```tsx
import { Switch } from "@nebutra/ui/primitives"
```

## Props

Extends `@radix-ui/react-switch` `Root` props.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `checked` | `boolean` | — | Controlled checked state |
| `defaultChecked` | `boolean` | `false` | Uncontrolled default checked state |
| `onCheckedChange` | `(checked: boolean) => void` | — | Callback when state changes |
| `disabled` | `boolean` | `false` | Disables the switch |
| `required` | `boolean` | `false` | Marks as required in form context |
| `name` | `string` | — | Form field name |
| `value` | `string` | `"on"` | Value submitted with form |
| `className` | `string` | — | Additional CSS classes |

## Examples

### Controlled Switch

<ComponentPreview className="min-h-[150px] flex items-center justify-center">
    <div className="flex items-center space-x-2">
    <Switch
      id="notifications"
    />
    <Label htmlFor="notifications">
      Notifications
    </Label>
  </div>

  ```tsx
<div className="flex items-center space-x-2">
  <Switch
    id="notifications"
  />
  <Label htmlFor="notifications">
    Notifications
  </Label>
</div>
```
</ComponentPreview>

### Settings List

<ComponentPreview className="min-h-[300px] flex items-center justify-center">
    <div className="space-y-4 w-full max-w-md p-6 border rounded-xl shadow-sm">
    <div className="flex items-center justify-between">
      <div>
        <Label htmlFor="wifi" className="text-base">Wi-Fi</Label>
        <p className="text-xs text-muted-foreground">Connect to local networks</p>
      </div>
      <Switch id="wifi" defaultChecked />
    </div>
    <div className="flex items-center justify-between">
      <div>
        <Label htmlFor="bluetooth" className="text-base">Bluetooth</Label>
        <p className="text-xs text-muted-foreground">Discover devices nearby</p>
      </div>
      <Switch id="bluetooth" />
    </div>
    <div className="flex items-center justify-between opacity-50">
      <div>
        <Label htmlFor="location" className="text-base cursor-not-allowed">Location</Label>
        <p className="text-xs text-muted-foreground">GPS tracking</p>
      </div>
      <Switch id="location" disabled />
    </div>
  </div>

  ```tsx
<div className="space-y-4 w-full max-w-md p-6 border rounded-xl shadow-sm">
  <div className="flex items-center justify-between">
    <div>
      <Label htmlFor="wifi" className="text-base">Wi-Fi</Label>
      <p className="text-xs text-muted-foreground">Connect to local networks</p>
    </div>
    <Switch id="wifi" defaultChecked />
  </div>
  <div className="flex items-center justify-between">
    <div>
      <Label htmlFor="bluetooth" className="text-base">Bluetooth</Label>
      <p className="text-xs text-muted-foreground">Discover devices nearby</p>
    </div>
    <Switch id="bluetooth" />
  </div>
  <div className="flex items-center justify-between opacity-50">
    <div>
      <Label htmlFor="location" className="text-base cursor-not-allowed">Location</Label>
      <p className="text-xs text-muted-foreground">GPS tracking</p>
    </div>
    <Switch id="location" disabled />
  </div>
</div>
```
</ComponentPreview>

### In a Form

```tsx
<FormField
  control={form.control}
  name="marketingEmails"
  render={({ field }) => (
    <FormItem className="flex items-center justify-between rounded-lg border p-4">
      <div>
        <FormLabel>Marketing emails</FormLabel>
        <FormDescription>Receive our latest news and offers.</FormDescription>
      </div>
      <FormControl>
        <Switch checked={field.value} onCheckedChange={field.onChange} />
      </FormControl>
    </FormItem>
  )}
/>
```

## Accessibility

- Built on Radix UI Switch — has `role="switch"` with `aria-checked`.
- Always pair with a `<Label>` linked via `htmlFor`/`id`.
- Keyboard: `Space` toggles the switch.
- Communicates state changes to screen readers via `aria-checked="true"` / `"false"`.
