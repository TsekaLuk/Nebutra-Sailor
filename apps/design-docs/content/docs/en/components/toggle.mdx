---
title: "Toggle"
description: "A two-state button that can be toggled on or off."
---

## Demo

<ComponentPreview name="toggle-demo" className="min-h-[150px] flex justify-center items-center" />


## Usage

```tsx
import { Toggle } from "@nebutra/ui";
```

## Props

| Prop             | Type                          | Default     | Description                                      |
| ---------------- | ----------------------------- | ----------- | ------------------------------------------------ |
| `pressed`        | `boolean`                     | —           | Controlled pressed state.                        |
| `defaultPressed` | `boolean`                     | `false`     | Initial pressed state (uncontrolled).            |
| `onPressedChange`| `(pressed: boolean) => void`  | —           | Callback when pressed state changes.             |
| `variant`        | `"default" \| "outline"`      | `"default"` | Visual variant.                                  |
| `size`           | `"default" \| "sm" \| "lg"`   | `"default"` | Size variant.                                    |
| `disabled`       | `boolean`                     | `false`     | Disables the toggle.                             |
| `asChild`        | `boolean`                     | `false`     | Render as child element.                         |

## Examples

### Outline Variant

<ComponentPreview name="toggle-2-demo" className="min-h-[100px] flex justify-center items-center" />


### Controlled

<ComponentPreview name="toggle-3-demo" className="min-h-[100px] flex justify-center items-center" />


### Text Toggle

<ComponentPreview name="toggle-4-demo" className="min-h-[100px] flex justify-center items-center" />


## Accessibility

Toggle renders a `<button>` with `aria-pressed`. Provide an `aria-label` when the toggle contains only an icon. Keyboard: `Space` or `Enter` activates.
