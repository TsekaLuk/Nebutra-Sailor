---
title: "Brand Colors"
description: "Complete Nebutra color system — primary blue, accent cyan, neutral scale, semantic colors, accessibility, and code references."
---

<Callout>
  All color values are sourced from the **云毓智能 品牌视觉识别手册**. These are the definitive specifications.
  Do not substitute with generic Tailwind palette colors.
</Callout>

<div className="mt-8">
  <ColorPalette />
</div>

<Tip>
  The blue undertone in the neutral scale is intentional. It creates a subtle visual connection to the brand blue.
  Never substitute with pure gray (e.g., Tailwind `gray-*` or `zinc-*`).
</Tip>

<ColorUsageDemos />

---

## Color in Code

### CSS Custom Properties

```css
:root {
  /* Brand */
  --color-brand-blue: #0033FE;
  --color-brand-cyan: #0BF1C3;

  /* Blue scale */
  --color-blue-50: #f0f4ff;
  --color-blue-100: #dbe4ff;
  --color-blue-200: #bac8ff;
  --color-blue-300: #91a7ff;
  --color-blue-400: #5c7cfa;
  --color-blue-500: #0033FE;
  --color-blue-600: #002ad4;
  --color-blue-700: #0021ab;
  --color-blue-800: #001882;
  --color-blue-900: #000f59;
  --color-blue-950: #000830;

  /* Cyan scale */
  --color-cyan-50: #e6fff8;
  --color-cyan-100: #b3ffec;
  --color-cyan-200: #80ffe0;
  --color-cyan-300: #4dfcd4;
  --color-cyan-400: #1af7c8;
  --color-cyan-500: #0BF1C3;
  --color-cyan-600: #09c9a3;
  --color-cyan-700: #07a183;
  --color-cyan-800: #057963;
  --color-cyan-900: #035143;
  --color-cyan-950: #012923;

  /* Semantic */
  --color-success: #22c55e;
  --color-warning: #f59e0b;
  --color-error: #ef4444;
  --color-info: #0033FE;
}
```

### Tailwind CSS Classes

```tsx
// Brand colors
<div className="bg-brand-blue-500 text-white" />
<div className="bg-brand-cyan-500 text-neutral-950" />

// Blue scale
<div className="bg-blue-50 text-blue-900" />
<div className="border-blue-200" />

// Semantic
<div className="bg-success text-white" />
<div className="bg-error text-white" />
<div className="bg-warning text-neutral-900" />

// Dark mode
<div className="bg-white dark:bg-neutral-950" />
<div className="text-neutral-900 dark:text-neutral-100" />
```

### JavaScript / TypeScript

```ts
import { colors } from "@nebutra/brand"

// Access brand colors programmatically
colors.blue[500]   // "#0033FE"
colors.cyan[500]   // "#0BF1C3"
colors.neutral[950] // "#020617"
```

---

## Prohibited Uses

<Warning>
  The following color substitutions are strictly forbidden:
</Warning>

| Prohibited | Reason |
|-----------|--------|
| Tailwind `sky-500` for brand blue | Sky blue is not Nebutra blue |
| Tailwind `teal-500` for brand cyan | Teal is not Nebutra cyan |
| Tailwind `gray-*` for neutrals | Nebutra neutrals have a blue undertone |
| Orange or red as primary action color | Only brand blue is used for primary actions |
| Cyan as text on light backgrounds | Fails WCAG contrast — use only on dark backgrounds |
| Brand blue as a flat background fill | Use the brand gradient for hero/CTA surfaces |
| Any non-brand color for `info` semantic | Info must use brand blue `#0033FE`, not sky/teal |

---

## Do / Don't

**Do** use `blue-500` (`#0033FE`) for all primary interactive elements (buttons, links, focus rings).

**Do** use the full 11-step scale for subtle tints, hover states, and dark mode adaptations.

**Do** pair `blue-500` with `cyan-500` exclusively in gradients — this is the brand signature.

**Do** use `neutral-950` as the dark mode page background.

**Don't** use cyan on white — it fails WCAG contrast.

**Don't** approximate brand colors with Tailwind defaults (`blue-600`, `emerald-400`).

**Don't** introduce new color scales outside the defined blue, cyan, neutral, and semantic palettes.

**Don't** use brand blue as a solid fill where the brand gradient is expected (hero sections, primary CTAs).
