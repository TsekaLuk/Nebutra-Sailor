---
title: "抽屉 (Drawer)"
description: "锚定在视口边缘的滑入式面板覆盖层，基于 Vaul 构建。"
---

## 演示 (Demo)

<ComponentPreview className="min-h-[300px] flex justify-center items-center">
  <Drawer>
    <DrawerTrigger asChild>
      <Button variant="outline">打开抽屉 (Open Drawer)</Button>
    </DrawerTrigger>
    <DrawerContent>
      <DrawerHeader>
        <DrawerTitle>编辑资料 (Edit Profile)</DrawerTitle>
        <DrawerDescription>
          在此处修改您的资料详细信息。 (Make changes to your profile here.)
        </DrawerDescription>
      </DrawerHeader>
      <div className="px-4 py-2">
        <p className="text-sm text-muted-foreground">抽屉主体内容。 (Drawer body content.)</p>
      </div>
      <DrawerFooter>
        <Button>保存更改 (Save changes)</Button>
        <DrawerClose asChild>
          <Button variant="outline">取消 (Cancel)</Button>
        </DrawerClose>
      </DrawerFooter>
    </DrawerContent>
  </Drawer>
</ComponentPreview>

## 使用方法 (Usage)

```tsx
import {
  Drawer,
  DrawerTrigger,
  DrawerContent,
  DrawerHeader,
  DrawerTitle,
  DrawerDescription,
  DrawerFooter,
  DrawerClose,
  DrawerPortal,
  DrawerOverlay,
} from "@nebutra/ui/primitives"
```

## 属性 (Props)

### Drawer (根组件)

| 属性 | 类型 | 默认值 | 描述 |
|------|------|---------|-------------|
| `open` | `boolean` | — | 受控的打开状态 |
| `onOpenChange` | `(open: boolean) => void` | — | 打开状态改变时的回调函数 |
| `direction` | `"top" \| "bottom" \| "left" \| "right"` | `"bottom"` | 滑入方向 |
| `shouldScaleBackground` | `boolean` | `false` | 抽屉打开时是否缩放背景 |

### DrawerContent (抽屉内容)

| 属性 | 类型 | 默认值 | 描述 |
|------|------|---------|-------------|
| `className` | `string` | — | 额外的 CSS 类名 |
| `children` | `ReactNode` | — | 抽屉内容 |

## 示例 (Examples)

### 受控抽屉 (Controlled Drawer)

```tsx
<ComponentPreview className="min-h-[200px] flex justify-center items-center">
  <Drawer>
    <DrawerTrigger asChild>
      <Button>打开设置 (Open Settings)</Button>
    </DrawerTrigger>
    <DrawerContent className="p-4">
      <DrawerHeader>
        <DrawerTitle>设置 (Settings)</DrawerTitle>
      </DrawerHeader>
      <DrawerFooter>
        <DrawerClose asChild>
          <Button variant="outline">关闭 (Close)</Button>
        </DrawerClose>
      </DrawerFooter>
    </DrawerContent>
  </Drawer>
</ComponentPreview>
```

### 右侧抽屉 (Right-Side Drawer)

```tsx
<ComponentPreview className="min-h-[250px] flex justify-center items-center">
  <Drawer direction="right">
    <DrawerTrigger asChild>
      <Button>打开侧边栏 (Open Sidebar)</Button>
    </DrawerTrigger>
    <DrawerContent className="w-80 h-full p-4 mt-0 ml-auto mr-0 mb-0">
      <DrawerHeader>
        <DrawerTitle>导航 (Navigation)</DrawerTitle>
      </DrawerHeader>
    </DrawerContent>
  </Drawer>
</ComponentPreview>
```

## 可访问性 (Accessibility)

- 基于 Vaul 构建 — 提供带有 `aria-modal="true"` 的 `role="dialog"`。
- `DrawerTitle` 通过 `aria-labelledby` 进行关联。
- 抽屉打开时，焦点被锁定在抽屉内。
- 按下 `Escape` 键可关闭抽屉并将焦点返回到触发器。
- 在移动设备上，支持拖拽关闭手势。
