---
title: "弹出框 (Popover)"
description: "锚定在触发元素上的浮动内容容器，基于 Radix UI 构建。"
---

## 演示 (Demo)

<ComponentPreview name="base-popover-demo" className="min-h-[300px] flex justify-center items-start pt-10" />

## 使用方法 (Usage)

<ComponentPreview name="popover-demo" className="min-h-[250px] flex justify-center items-start pt-10">
    <Popover>
    <PopoverTrigger asChild>
      <Button variant="ghost" size="icon">
        <Settings className="h-4 w-4" />
      </Button>
    </PopoverTrigger>
    <PopoverContent side="right" align="start" className="w-64">
      <div className="space-y-3">
        <div className="flex items-center justify-between">
          <Label htmlFor="notifications">通知 (Notifications)</Label>
          <Switch id="notifications" />
        </div>
        <div className="flex items-center justify-between">
          <Label htmlFor="marketing">营销邮件 (Marketing emails)</Label>
          <Switch id="marketing" />
        </div>
      </div>
    </PopoverContent>
  </Popover />

### 受控弹出框 (Controlled Popover)

<ComponentPreview name="popover-2-demo" className="min-h-[250px] flex justify-center items-start pt-10">
    <Popover defaultOpen={true}>
    <PopoverTrigger asChild>
      <Button>切换弹出框 (Toggle Popover)</Button>
    </PopoverTrigger>
    <PopoverContent>
      <p className="mb-4 text-sm">受控弹出框内容。 (Controlled popover content.)</p>
      <Button variant="outline" size="sm">关闭 (Close)</Button>
    </PopoverContent>
  </Popover />

## 可访问性 (Accessibility)

- 基于 Radix UI Popover 构建 —— `PopoverContent` 具有 `role="dialog"` 属性。
- 触发器具有 `aria-expanded` 和 `aria-haspopup="dialog"` 属性。
- 打开时焦点移入弹出框；关闭时焦点返回触发器。
- 按下 `Escape` 键关闭弹出框。
- 默认情况下，点击弹出框外部会将其关闭。
