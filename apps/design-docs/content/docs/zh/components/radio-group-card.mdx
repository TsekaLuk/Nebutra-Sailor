---
title: "卡片式单选组 (Radio Group Card)"
description: "卡片样式的单选按钮组变体，用于提供视觉表现力更丰富的选项选择。"
---

## 演示 (Demo)

<ComponentPreview name="radio-group-card-demo"  />


## 使用方法 (Usage)

```tsx
import { RadioGroupCard, RadioGroupCardItem } from "@nebutra/ui/primitives"
```

## 属性 (Props)

### RadioGroupCard (单选组)

| 属性 (Prop) | 类型 | 默认值 | 描述 |
|------|------|---------|-------------|
| `value` | `string` | — | 受控模式下的选中值 |
| `defaultValue` | `string` | — | 非受控模式下的默认选中值 |
| `onValueChange` | `(value: string) => void` | — | 选中项改变时的回调函数 |
| `disabled` | `boolean` | `false` | 禁用所有选项 |
| `className` | `string` | — | 额外的 CSS 类名（建议使用 `grid` 工具类设置布局） |
| `children` | `ReactNode` | — | `RadioGroupCardItem` 元素集合 |

### RadioGroupCardItem (单选卡片项)

| 属性 (Prop) | 类型 | 默认值 | 描述 |
|------|------|---------|-------------|
| `value` | `string` | — | **必需。** 该选项对应的数值 |
| `id` | `string` | — | 用于无障碍标签关联的 ID |
| `disabled` | `boolean` | `false` | 禁用该特定选项 |
| `className` | `string` | — | 额外的 CSS 类名 |
| `children` | `ReactNode` | — | 卡片内容（通常为图标 + 标签 + 描述） |

## 示例 (Examples)

### 价格方案选择器 (Pricing Plan Selector)

```tsx
<RadioGroupCard defaultValue="pro" className="grid grid-cols-3 gap-4">
  <RadioGroupCardItem value="starter" id="plan-starter">
    <div className="font-semibold">入门版 (Starter)</div>
    <div className="text-2xl font-bold mt-1">$0</div>
    <div className="text-xs text-muted-foreground">每月</div>
  </RadioGroupCardItem>
  <RadioGroupCardItem value="pro" id="plan-pro">
    <div className="font-semibold">专业版 (Pro)</div>
    <div className="text-2xl font-bold mt-1">$29</div>
    <div className="text-xs text-muted-foreground">每月</div>
  </RadioGroupCardItem>
  <RadioGroupCardItem value="enterprise" id="plan-enterprise">
    <div className="font-semibold">企业版 (Enterprise)</div>
    <div className="text-2xl font-bold mt-1">定制</div>
    <div className="text-xs text-muted-foreground">联系销售</div>
  </RadioGroupCardItem>
</RadioGroupCard>
```

## 无障碍支持 (Accessibility)

- 选中项具有明显的选中指示符及边框高亮。
- 每个卡片项均带有 `role="radio"` 属性以及 `aria-checked` 状态。
- 键盘操作：使用方向键 (`ArrowLeft`/`ArrowRight` 或 `ArrowUp`/`ArrowDown`) 在卡片间切换。
- 使用 `Space` 键选中当前聚焦的卡片。
走
