---
title: Story 组件故事
description: 使用 Fumadocs Story 展示组件的不同变体和交互控制。
---

import { story } from '@/components/stories/callout.story';

## Badge 组件

使用 `<story.WithControl />` 可以渲染组件的交互式预览，支持切换不同变体和动态调整 props。

<story.WithControl />
