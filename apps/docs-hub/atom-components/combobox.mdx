---
title: "Combobox"
description: "Searchable select input combining a text field with a dropdown list of options."
---

## Demo

```tsx
import { useState } from "react"
import { Check, ChevronsUpDown } from "lucide-react"
import { Button } from "@nebutra/custom-ui/primitives"
import { Command, CommandEmpty, CommandGroup, CommandInput, CommandItem } from "@nebutra/custom-ui/primitives"
import { Popover, PopoverContent, PopoverTrigger } from "@nebutra/custom-ui/primitives"
import { cn } from "@nebutra/design-system/utils"

const frameworks = [
  { value: "next", label: "Next.js" },
  { value: "remix", label: "Remix" },
  { value: "astro", label: "Astro" },
  { value: "nuxt", label: "Nuxt" },
]

export function ComboboxDemo() {
  const [open, setOpen] = useState(false)
  const [value, setValue] = useState("")

  return (
    <Popover open={open} onOpenChange={setOpen}>
      <PopoverTrigger asChild>
        <Button
          variant="outline"
          role="combobox"
          aria-expanded={open}
          className="w-48 justify-between"
        >
          {value
            ? frameworks.find((f) => f.value === value)?.label
            : "Select framework..."}
          <ChevronsUpDown className="ml-2 h-4 w-4 shrink-0 opacity-50" />
        </Button>
      </PopoverTrigger>
      <PopoverContent className="w-48 p-0">
        <Command>
          <CommandInput placeholder="Search framework..." />
          <CommandEmpty>No framework found.</CommandEmpty>
          <CommandGroup>
            {frameworks.map((framework) => (
              <CommandItem
                key={framework.value}
                value={framework.value}
                onSelect={(current) => {
                  setValue(current === value ? "" : current)
                  setOpen(false)
                }}
              >
                <Check className={cn("mr-2 h-4 w-4", value === framework.value ? "opacity-100" : "opacity-0")} />
                {framework.label}
              </CommandItem>
            ))}
          </CommandGroup>
        </Command>
      </PopoverContent>
    </Popover>
  )
}
```

## Usage

```tsx
import { Popover, PopoverContent, PopoverTrigger } from "@nebutra/custom-ui/primitives"
import { Command, CommandEmpty, CommandGroup, CommandInput, CommandItem } from "@nebutra/custom-ui/primitives"
```

<Info>
  Combobox is a pattern built from `Popover` + `Command`. There is no standalone
  `Combobox` export — compose these primitives directly.
</Info>

## Props

### Key Component Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `open` | `boolean` | — | Controls dropdown visibility |
| `onOpenChange` | `(open: boolean) => void` | — | Callback when open state changes |
| `value` | `string` | — | Currently selected value |
| `className` | `string` | — | Additional CSS classes |

## Examples

### With Async Search

```tsx
const [query, setQuery] = useState("")
const results = useSearch(query)

<Command>
  <CommandInput value={query} onValueChange={setQuery} placeholder="Search..." />
  <CommandGroup>
    {results.map((r) => (
      <CommandItem key={r.id} value={r.id} onSelect={handleSelect}>
        {r.name}
      </CommandItem>
    ))}
  </CommandGroup>
</Command>
```

## Accessibility

- Trigger button has `role="combobox"` with `aria-expanded`.
- `CommandInput` provides a live search accessible to screen readers.
- `CommandItem` elements have `role="option"` with `aria-selected`.
- Keyboard navigation: type to filter, `ArrowUp`/`ArrowDown` to navigate, `Enter` to select, `Escape` to close.
