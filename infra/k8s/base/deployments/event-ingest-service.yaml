apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-ingest-service
  namespace: nebutra
  labels:
    app.kubernetes.io/name: event-ingest-service
    app.kubernetes.io/component: service
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: event-ingest-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: event-ingest-service
        app.kubernetes.io/component: service
    spec:
      terminationGracePeriodSeconds: 60
      containers:
        - name: event-ingest-service
          # CI pipeline overrides this tag via kustomize image transforms
          image: nebutra/event-ingest-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8008
              protocol: TCP
          envFrom:
            - configMapRef:
                name: nebutra-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DATABASE_URL
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: REDIS_URL
            # ClickHouse is used as the primary event store for analytics ingestion
            - name: CLICKHOUSE_HOST
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: CLICKHOUSE_HOST
            - name: CLICKHOUSE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: CLICKHOUSE_PASSWORD
          resources:
            # Event ingestion handles high-throughput writes; extra CPU helps with batching
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
      securityContext:
        fsGroup: 1000
